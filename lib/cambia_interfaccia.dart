import 'package:flutter/material.dart';
import 'package:bollino/metodi_database.dart';
class Cambia_Interfaccia extends StatelessWidget {
  const Cambia_Interfaccia({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    if(true)
    return Container();
  }
}
